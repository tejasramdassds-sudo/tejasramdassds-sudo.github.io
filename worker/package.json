{
  "name": "tejas-website-event-logger",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create tejas-website-events",
    "d1:migrate:local": "wrangler d1 migrations apply tejas-website-events --local",
    "d1:migrate:remote": "wrangler d1 migrations apply tejas-website-events --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
